---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2026 The Linux Foundation

# No path-specific overrides are required; the test workflow uses only
# inputs that the action defines, so actionlint needs no ignore rules.
paths: {}
